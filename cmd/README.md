```text
    1、获取社区issue类型列表
    issue-cli get it
    2、获取issue类型模板
    issue-cli get it -n [类型名称]
    3、获取可创建issue仓库数据
    issue-cli get repo
    4、创建issue
    issue-cli create issue -f [文件路径] -t [issue title] -i [issue类型id] -r [仓库id]
```